define(['logicsclientportal'], function(logicsclientportal) {
    'use strict';
    logicsclientportal.register.controller('indexcontroller', ['$scope', '$state', '$rootScope',
        function(sc, st, rs) {}
    ]);
});
